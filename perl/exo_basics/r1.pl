#! /usr/bin/perl
use Modern::Perl;
use YAML;

$param->{foo} = $fontsize;
$param->{bar} = $fontsize;
$param->{bing} = $fontsize;
$param->{bang} = $fontsize;
















# $_ = $fontsize for @$param{qw/ foo bar bing bang /};
